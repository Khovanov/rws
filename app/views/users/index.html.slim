h3 Listing users

table.striped
  thead
    tr
      th Name
      th
      th
      th
  tbody
    = render partial: 'users', collection: @users, as: 'user'

p = link_to 'New User', new_user_path, class: 'btn'