h3 Listing tickets

table.striped
  thead
    tr
      th Stations
      th
      th
      th
  tbody
    = render partial: 'tickets', collection: @tickets.where(user: current_user), as: 'ticket'
